#include "monty.h"

/**
 * nop - the func does nothing
 * @stack: the stack
 * @line_number: the line number
 * Return: returns nothing
*/

void nop(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
